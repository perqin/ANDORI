package com.perqin.andori.ui.shortcuts

import androidx.core.app.Person

const val SHORTCUT_ID_FLOATING = "floating"

const val SHORTCUT_CATEGORY_FLOATING = "com.perqin.andori.category.FLOATING"

val rinkoPerson = Person.Builder()
    .setName("Rinko")
    .setKey("rinko")
    .build()
